all:
  children:
    k8s_cluster:
      hosts:
        localhost:
          ansible_connection: local
          k8s_host: localhost
          ansible_python_interpreter: "{{ ansible_playbook_python }}"
          namespace: audio
          env: test
